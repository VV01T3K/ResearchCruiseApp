# Database Configuration
DATABASE_SEED_AUTOMATICALLY=true
DATABASE_MIGRATE_AUTOMATICALLY=true
DATABASE_LOG_USER_PASSWORDS_WHEN_SEEDING=true

# Database Connection
DB_SERVER=researchcruiseapp-db
DB_PORT=1433
DB_NAME=ResearchCruiseApp
DB_USER=sa
DB_PASSWORD=p@ssw0rd

# Frontend Configuration
FRONTEND_URL=https://cruise.wsiwiec.com

# HyperDX (self-hosted): get key from https://hyperdx.wsiwiec.com → Team Settings.
# Backend uses HYPERDX_API_KEY at runtime. Frontend needs it at IMAGE BUILD time
# (Vite inlines it), so it must also be passed as a --build-arg in CI.
USE_OTLP_EXPORTER=false # set to true to enable otel
HYPERDX_API_KEY=
OTLP_EXPORTER_ENDPOINT=https://otel.wsiwiec.com
