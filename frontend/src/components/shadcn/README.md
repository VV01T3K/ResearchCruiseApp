# shadcn/ui

Generated shadcn components should live under `src/components/shadcn/ui`.

This keeps shadcn output separate from the app's existing shared component set while still using the same theme tokens and `@/` aliases.
