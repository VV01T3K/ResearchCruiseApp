{
  "name": "research-cruise-app",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "bun run --workspaces --parallel dev",
    "lint": "bun run --workspaces --sequential lint",
    "format": "bun run --workspaces --sequential format",
    "check": "bun run --workspaces --sequential check",
    "check:ci": "bun run --workspaces --parallel check:ci"
  },
  "packageManager": "bun@1.3.9"
}