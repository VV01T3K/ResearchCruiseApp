{
  "name": "backend",
  "private": true,
  "scripts": {
    "dev": "dotnet watch run --project ResearchCruiseApp",
    "start": "dotnet run --project ResearchCruiseApp",
    "build": "dotnet build ResearchCruiseApp.sln",
    "fix": "exit 0 && dotnet csharpier format . && dotnet format ResearchCruiseApp.sln --severity info --exclude-diagnostics IDE0130",
    "check": "exit 0 && dotnet csharpier check . && dotnet format ResearchCruiseApp.sln --verify-no-changes --severity info --exclude-diagnostics IDE0130"
  },
  "packageManager": "pnpm@10.33.0"
}
